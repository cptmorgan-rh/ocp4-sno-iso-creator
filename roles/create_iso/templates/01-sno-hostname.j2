variant: openshift
version: 4.10.0
metadata:
  name: 01-master-hostname
  labels:
    machineconfiguration.openshift.io/role: master
storage:
  files:
    - path: /etc/hostname
      mode: 0644
      overwrite: true
      contents:
        inline: |
          {{ sno_vm.name }}
